- Fixed a watch-handler leak in `WorkerRegistry`. `BaseWorker`'s ready-wait registered a fresh handler for each worker name on every job request and never removed it, so `request_job`, `request_job_group` and `job_group` each left one behind, on the ready path as well as the timeout path. The registry now has an `unwatch()` and the ready-wait removes the handlers it registered.
