- Fixed `GoogleSTTService` dropping the transcript that arrives as its four minute streaming limit is reached. The limit was checked before the response Google had already delivered was handled, so a final transcript landing on that boundary was discarded on the way out to reconnect. The reconnect itself is unchanged.
